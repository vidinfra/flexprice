// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/flexprice/flexprice/ent/costsheet"
	"github.com/flexprice/flexprice/ent/meter"
	"github.com/flexprice/flexprice/ent/predicate"
	"github.com/flexprice/flexprice/ent/price"
)

// CostsheetQuery is the builder for querying Costsheet entities.
type CostsheetQuery struct {
	config
	ctx        *QueryContext
	order      []costsheet.OrderOption
	inters     []Interceptor
	predicates []predicate.Costsheet
	withMeter  *MeterQuery
	withPrice  *PriceQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CostsheetQuery builder.
func (cq *CostsheetQuery) Where(ps ...predicate.Costsheet) *CostsheetQuery {
	cq.predicates = append(cq.predicates, ps...)
	return cq
}

// Limit the number of records to be returned by this query.
func (cq *CostsheetQuery) Limit(limit int) *CostsheetQuery {
	cq.ctx.Limit = &limit
	return cq
}

// Offset to start from.
func (cq *CostsheetQuery) Offset(offset int) *CostsheetQuery {
	cq.ctx.Offset = &offset
	return cq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cq *CostsheetQuery) Unique(unique bool) *CostsheetQuery {
	cq.ctx.Unique = &unique
	return cq
}

// Order specifies how the records should be ordered.
func (cq *CostsheetQuery) Order(o ...costsheet.OrderOption) *CostsheetQuery {
	cq.order = append(cq.order, o...)
	return cq
}

// QueryMeter chains the current query on the "meter" edge.
func (cq *CostsheetQuery) QueryMeter() *MeterQuery {
	query := (&MeterClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(costsheet.Table, costsheet.FieldID, selector),
			sqlgraph.To(meter.Table, meter.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, costsheet.MeterTable, costsheet.MeterColumn),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPrice chains the current query on the "price" edge.
func (cq *CostsheetQuery) QueryPrice() *PriceQuery {
	query := (&PriceClient{config: cq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(costsheet.Table, costsheet.FieldID, selector),
			sqlgraph.To(price.Table, price.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, costsheet.PriceTable, costsheet.PriceColumn),
		)
		fromU = sqlgraph.SetNeighbors(cq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Costsheet entity from the query.
// Returns a *NotFoundError when no Costsheet was found.
func (cq *CostsheetQuery) First(ctx context.Context) (*Costsheet, error) {
	nodes, err := cq.Limit(1).All(setContextOp(ctx, cq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{costsheet.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cq *CostsheetQuery) FirstX(ctx context.Context) *Costsheet {
	node, err := cq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Costsheet ID from the query.
// Returns a *NotFoundError when no Costsheet ID was found.
func (cq *CostsheetQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = cq.Limit(1).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{costsheet.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cq *CostsheetQuery) FirstIDX(ctx context.Context) string {
	id, err := cq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Costsheet entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Costsheet entity is found.
// Returns a *NotFoundError when no Costsheet entities are found.
func (cq *CostsheetQuery) Only(ctx context.Context) (*Costsheet, error) {
	nodes, err := cq.Limit(2).All(setContextOp(ctx, cq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{costsheet.Label}
	default:
		return nil, &NotSingularError{costsheet.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cq *CostsheetQuery) OnlyX(ctx context.Context) *Costsheet {
	node, err := cq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Costsheet ID in the query.
// Returns a *NotSingularError when more than one Costsheet ID is found.
// Returns a *NotFoundError when no entities are found.
func (cq *CostsheetQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = cq.Limit(2).IDs(setContextOp(ctx, cq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{costsheet.Label}
	default:
		err = &NotSingularError{costsheet.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cq *CostsheetQuery) OnlyIDX(ctx context.Context) string {
	id, err := cq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Costsheets.
func (cq *CostsheetQuery) All(ctx context.Context) ([]*Costsheet, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryAll)
	if err := cq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Costsheet, *CostsheetQuery]()
	return withInterceptors[[]*Costsheet](ctx, cq, qr, cq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cq *CostsheetQuery) AllX(ctx context.Context) []*Costsheet {
	nodes, err := cq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Costsheet IDs.
func (cq *CostsheetQuery) IDs(ctx context.Context) (ids []string, err error) {
	if cq.ctx.Unique == nil && cq.path != nil {
		cq.Unique(true)
	}
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryIDs)
	if err = cq.Select(costsheet.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cq *CostsheetQuery) IDsX(ctx context.Context) []string {
	ids, err := cq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cq *CostsheetQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryCount)
	if err := cq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cq, querierCount[*CostsheetQuery](), cq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cq *CostsheetQuery) CountX(ctx context.Context) int {
	count, err := cq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cq *CostsheetQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cq.ctx, ent.OpQueryExist)
	switch _, err := cq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cq *CostsheetQuery) ExistX(ctx context.Context) bool {
	exist, err := cq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CostsheetQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cq *CostsheetQuery) Clone() *CostsheetQuery {
	if cq == nil {
		return nil
	}
	return &CostsheetQuery{
		config:     cq.config,
		ctx:        cq.ctx.Clone(),
		order:      append([]costsheet.OrderOption{}, cq.order...),
		inters:     append([]Interceptor{}, cq.inters...),
		predicates: append([]predicate.Costsheet{}, cq.predicates...),
		withMeter:  cq.withMeter.Clone(),
		withPrice:  cq.withPrice.Clone(),
		// clone intermediate query.
		sql:  cq.sql.Clone(),
		path: cq.path,
	}
}

// WithMeter tells the query-builder to eager-load the nodes that are connected to
// the "meter" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CostsheetQuery) WithMeter(opts ...func(*MeterQuery)) *CostsheetQuery {
	query := (&MeterClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withMeter = query
	return cq
}

// WithPrice tells the query-builder to eager-load the nodes that are connected to
// the "price" edge. The optional arguments are used to configure the query builder of the edge.
func (cq *CostsheetQuery) WithPrice(opts ...func(*PriceQuery)) *CostsheetQuery {
	query := (&PriceClient{config: cq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cq.withPrice = query
	return cq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Costsheet.Query().
//		GroupBy(costsheet.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cq *CostsheetQuery) GroupBy(field string, fields ...string) *CostsheetGroupBy {
	cq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CostsheetGroupBy{build: cq}
	grbuild.flds = &cq.ctx.Fields
	grbuild.label = costsheet.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.Costsheet.Query().
//		Select(costsheet.FieldTenantID).
//		Scan(ctx, &v)
func (cq *CostsheetQuery) Select(fields ...string) *CostsheetSelect {
	cq.ctx.Fields = append(cq.ctx.Fields, fields...)
	sbuild := &CostsheetSelect{CostsheetQuery: cq}
	sbuild.label = costsheet.Label
	sbuild.flds, sbuild.scan = &cq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CostsheetSelect configured with the given aggregations.
func (cq *CostsheetQuery) Aggregate(fns ...AggregateFunc) *CostsheetSelect {
	return cq.Select().Aggregate(fns...)
}

func (cq *CostsheetQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cq); err != nil {
				return err
			}
		}
	}
	for _, f := range cq.ctx.Fields {
		if !costsheet.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cq.path != nil {
		prev, err := cq.path(ctx)
		if err != nil {
			return err
		}
		cq.sql = prev
	}
	return nil
}

func (cq *CostsheetQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Costsheet, error) {
	var (
		nodes       = []*Costsheet{}
		_spec       = cq.querySpec()
		loadedTypes = [2]bool{
			cq.withMeter != nil,
			cq.withPrice != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Costsheet).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Costsheet{config: cq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cq.withMeter; query != nil {
		if err := cq.loadMeter(ctx, query, nodes, nil,
			func(n *Costsheet, e *Meter) { n.Edges.Meter = e }); err != nil {
			return nil, err
		}
	}
	if query := cq.withPrice; query != nil {
		if err := cq.loadPrice(ctx, query, nodes, nil,
			func(n *Costsheet, e *Price) { n.Edges.Price = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cq *CostsheetQuery) loadMeter(ctx context.Context, query *MeterQuery, nodes []*Costsheet, init func(*Costsheet), assign func(*Costsheet, *Meter)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Costsheet)
	for i := range nodes {
		fk := nodes[i].MeterID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(meter.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "meter_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (cq *CostsheetQuery) loadPrice(ctx context.Context, query *PriceQuery, nodes []*Costsheet, init func(*Costsheet), assign func(*Costsheet, *Price)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Costsheet)
	for i := range nodes {
		fk := nodes[i].PriceID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(price.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "price_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (cq *CostsheetQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cq.querySpec()
	_spec.Node.Columns = cq.ctx.Fields
	if len(cq.ctx.Fields) > 0 {
		_spec.Unique = cq.ctx.Unique != nil && *cq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cq.driver, _spec)
}

func (cq *CostsheetQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(costsheet.Table, costsheet.Columns, sqlgraph.NewFieldSpec(costsheet.FieldID, field.TypeString))
	_spec.From = cq.sql
	if unique := cq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cq.path != nil {
		_spec.Unique = true
	}
	if fields := cq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, costsheet.FieldID)
		for i := range fields {
			if fields[i] != costsheet.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if cq.withMeter != nil {
			_spec.Node.AddColumnOnce(costsheet.FieldMeterID)
		}
		if cq.withPrice != nil {
			_spec.Node.AddColumnOnce(costsheet.FieldPriceID)
		}
	}
	if ps := cq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cq *CostsheetQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cq.driver.Dialect())
	t1 := builder.Table(costsheet.Table)
	columns := cq.ctx.Fields
	if len(columns) == 0 {
		columns = costsheet.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cq.sql != nil {
		selector = cq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cq.ctx.Unique != nil && *cq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cq.predicates {
		p(selector)
	}
	for _, p := range cq.order {
		p(selector)
	}
	if offset := cq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CostsheetGroupBy is the group-by builder for Costsheet entities.
type CostsheetGroupBy struct {
	selector
	build *CostsheetQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cgb *CostsheetGroupBy) Aggregate(fns ...AggregateFunc) *CostsheetGroupBy {
	cgb.fns = append(cgb.fns, fns...)
	return cgb
}

// Scan applies the selector query and scans the result into the given value.
func (cgb *CostsheetGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cgb.build.ctx, ent.OpQueryGroupBy)
	if err := cgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CostsheetQuery, *CostsheetGroupBy](ctx, cgb.build, cgb, cgb.build.inters, v)
}

func (cgb *CostsheetGroupBy) sqlScan(ctx context.Context, root *CostsheetQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cgb.fns))
	for _, fn := range cgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cgb.flds)+len(cgb.fns))
		for _, f := range *cgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CostsheetSelect is the builder for selecting fields of Costsheet entities.
type CostsheetSelect struct {
	*CostsheetQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cs *CostsheetSelect) Aggregate(fns ...AggregateFunc) *CostsheetSelect {
	cs.fns = append(cs.fns, fns...)
	return cs
}

// Scan applies the selector query and scans the result into the given value.
func (cs *CostsheetSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cs.ctx, ent.OpQuerySelect)
	if err := cs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CostsheetQuery, *CostsheetSelect](ctx, cs.CostsheetQuery, cs, cs.inters, v)
}

func (cs *CostsheetSelect) sqlScan(ctx context.Context, root *CostsheetQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cs.fns))
	for _, fn := range cs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
