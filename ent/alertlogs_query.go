// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/flexprice/flexprice/ent/alertlogs"
	"github.com/flexprice/flexprice/ent/predicate"
)

// AlertLogsQuery is the builder for querying AlertLogs entities.
type AlertLogsQuery struct {
	config
	ctx        *QueryContext
	order      []alertlogs.OrderOption
	inters     []Interceptor
	predicates []predicate.AlertLogs
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AlertLogsQuery builder.
func (alq *AlertLogsQuery) Where(ps ...predicate.AlertLogs) *AlertLogsQuery {
	alq.predicates = append(alq.predicates, ps...)
	return alq
}

// Limit the number of records to be returned by this query.
func (alq *AlertLogsQuery) Limit(limit int) *AlertLogsQuery {
	alq.ctx.Limit = &limit
	return alq
}

// Offset to start from.
func (alq *AlertLogsQuery) Offset(offset int) *AlertLogsQuery {
	alq.ctx.Offset = &offset
	return alq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (alq *AlertLogsQuery) Unique(unique bool) *AlertLogsQuery {
	alq.ctx.Unique = &unique
	return alq
}

// Order specifies how the records should be ordered.
func (alq *AlertLogsQuery) Order(o ...alertlogs.OrderOption) *AlertLogsQuery {
	alq.order = append(alq.order, o...)
	return alq
}

// First returns the first AlertLogs entity from the query.
// Returns a *NotFoundError when no AlertLogs was found.
func (alq *AlertLogsQuery) First(ctx context.Context) (*AlertLogs, error) {
	nodes, err := alq.Limit(1).All(setContextOp(ctx, alq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{alertlogs.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (alq *AlertLogsQuery) FirstX(ctx context.Context) *AlertLogs {
	node, err := alq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AlertLogs ID from the query.
// Returns a *NotFoundError when no AlertLogs ID was found.
func (alq *AlertLogsQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = alq.Limit(1).IDs(setContextOp(ctx, alq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{alertlogs.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (alq *AlertLogsQuery) FirstIDX(ctx context.Context) string {
	id, err := alq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AlertLogs entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AlertLogs entity is found.
// Returns a *NotFoundError when no AlertLogs entities are found.
func (alq *AlertLogsQuery) Only(ctx context.Context) (*AlertLogs, error) {
	nodes, err := alq.Limit(2).All(setContextOp(ctx, alq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{alertlogs.Label}
	default:
		return nil, &NotSingularError{alertlogs.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (alq *AlertLogsQuery) OnlyX(ctx context.Context) *AlertLogs {
	node, err := alq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AlertLogs ID in the query.
// Returns a *NotSingularError when more than one AlertLogs ID is found.
// Returns a *NotFoundError when no entities are found.
func (alq *AlertLogsQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = alq.Limit(2).IDs(setContextOp(ctx, alq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{alertlogs.Label}
	default:
		err = &NotSingularError{alertlogs.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (alq *AlertLogsQuery) OnlyIDX(ctx context.Context) string {
	id, err := alq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AlertLogsSlice.
func (alq *AlertLogsQuery) All(ctx context.Context) ([]*AlertLogs, error) {
	ctx = setContextOp(ctx, alq.ctx, ent.OpQueryAll)
	if err := alq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AlertLogs, *AlertLogsQuery]()
	return withInterceptors[[]*AlertLogs](ctx, alq, qr, alq.inters)
}

// AllX is like All, but panics if an error occurs.
func (alq *AlertLogsQuery) AllX(ctx context.Context) []*AlertLogs {
	nodes, err := alq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AlertLogs IDs.
func (alq *AlertLogsQuery) IDs(ctx context.Context) (ids []string, err error) {
	if alq.ctx.Unique == nil && alq.path != nil {
		alq.Unique(true)
	}
	ctx = setContextOp(ctx, alq.ctx, ent.OpQueryIDs)
	if err = alq.Select(alertlogs.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (alq *AlertLogsQuery) IDsX(ctx context.Context) []string {
	ids, err := alq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (alq *AlertLogsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, alq.ctx, ent.OpQueryCount)
	if err := alq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, alq, querierCount[*AlertLogsQuery](), alq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (alq *AlertLogsQuery) CountX(ctx context.Context) int {
	count, err := alq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (alq *AlertLogsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, alq.ctx, ent.OpQueryExist)
	switch _, err := alq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (alq *AlertLogsQuery) ExistX(ctx context.Context) bool {
	exist, err := alq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AlertLogsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (alq *AlertLogsQuery) Clone() *AlertLogsQuery {
	if alq == nil {
		return nil
	}
	return &AlertLogsQuery{
		config:     alq.config,
		ctx:        alq.ctx.Clone(),
		order:      append([]alertlogs.OrderOption{}, alq.order...),
		inters:     append([]Interceptor{}, alq.inters...),
		predicates: append([]predicate.AlertLogs{}, alq.predicates...),
		// clone intermediate query.
		sql:  alq.sql.Clone(),
		path: alq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AlertLogs.Query().
//		GroupBy(alertlogs.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (alq *AlertLogsQuery) GroupBy(field string, fields ...string) *AlertLogsGroupBy {
	alq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AlertLogsGroupBy{build: alq}
	grbuild.flds = &alq.ctx.Fields
	grbuild.label = alertlogs.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.AlertLogs.Query().
//		Select(alertlogs.FieldTenantID).
//		Scan(ctx, &v)
func (alq *AlertLogsQuery) Select(fields ...string) *AlertLogsSelect {
	alq.ctx.Fields = append(alq.ctx.Fields, fields...)
	sbuild := &AlertLogsSelect{AlertLogsQuery: alq}
	sbuild.label = alertlogs.Label
	sbuild.flds, sbuild.scan = &alq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AlertLogsSelect configured with the given aggregations.
func (alq *AlertLogsQuery) Aggregate(fns ...AggregateFunc) *AlertLogsSelect {
	return alq.Select().Aggregate(fns...)
}

func (alq *AlertLogsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range alq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, alq); err != nil {
				return err
			}
		}
	}
	for _, f := range alq.ctx.Fields {
		if !alertlogs.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if alq.path != nil {
		prev, err := alq.path(ctx)
		if err != nil {
			return err
		}
		alq.sql = prev
	}
	return nil
}

func (alq *AlertLogsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AlertLogs, error) {
	var (
		nodes = []*AlertLogs{}
		_spec = alq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AlertLogs).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AlertLogs{config: alq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, alq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (alq *AlertLogsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := alq.querySpec()
	_spec.Node.Columns = alq.ctx.Fields
	if len(alq.ctx.Fields) > 0 {
		_spec.Unique = alq.ctx.Unique != nil && *alq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, alq.driver, _spec)
}

func (alq *AlertLogsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(alertlogs.Table, alertlogs.Columns, sqlgraph.NewFieldSpec(alertlogs.FieldID, field.TypeString))
	_spec.From = alq.sql
	if unique := alq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if alq.path != nil {
		_spec.Unique = true
	}
	if fields := alq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, alertlogs.FieldID)
		for i := range fields {
			if fields[i] != alertlogs.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := alq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := alq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := alq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := alq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (alq *AlertLogsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(alq.driver.Dialect())
	t1 := builder.Table(alertlogs.Table)
	columns := alq.ctx.Fields
	if len(columns) == 0 {
		columns = alertlogs.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if alq.sql != nil {
		selector = alq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if alq.ctx.Unique != nil && *alq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range alq.predicates {
		p(selector)
	}
	for _, p := range alq.order {
		p(selector)
	}
	if offset := alq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := alq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AlertLogsGroupBy is the group-by builder for AlertLogs entities.
type AlertLogsGroupBy struct {
	selector
	build *AlertLogsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (algb *AlertLogsGroupBy) Aggregate(fns ...AggregateFunc) *AlertLogsGroupBy {
	algb.fns = append(algb.fns, fns...)
	return algb
}

// Scan applies the selector query and scans the result into the given value.
func (algb *AlertLogsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, algb.build.ctx, ent.OpQueryGroupBy)
	if err := algb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AlertLogsQuery, *AlertLogsGroupBy](ctx, algb.build, algb, algb.build.inters, v)
}

func (algb *AlertLogsGroupBy) sqlScan(ctx context.Context, root *AlertLogsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(algb.fns))
	for _, fn := range algb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*algb.flds)+len(algb.fns))
		for _, f := range *algb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*algb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := algb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AlertLogsSelect is the builder for selecting fields of AlertLogs entities.
type AlertLogsSelect struct {
	*AlertLogsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (als *AlertLogsSelect) Aggregate(fns ...AggregateFunc) *AlertLogsSelect {
	als.fns = append(als.fns, fns...)
	return als
}

// Scan applies the selector query and scans the result into the given value.
func (als *AlertLogsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, als.ctx, ent.OpQuerySelect)
	if err := als.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AlertLogsQuery, *AlertLogsSelect](ctx, als.AlertLogsQuery, als, als.inters, v)
}

func (als *AlertLogsSelect) sqlScan(ctx context.Context, root *AlertLogsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(als.fns))
	for _, fn := range als.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*als.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := als.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
